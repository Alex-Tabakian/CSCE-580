1. TC-identifier:

TC2

2. TC-name:

NegativeSentiment-NegationContrast

3. TC-objective:

Evaluate whether model correctly handles negation and contrasting tone.

4. TC-input:

I wanted to like this film, but it was dull and disappointing.

5. TC-reference-output:

Negative (true_label = 0)

6. TC-harm-risk-info:

HC1-incorrect-sentiment-label (false positive could cause incorrect positive bias)

7. TC-other-info:

negation/contrast
Predictions → TF-IDF: 0, DistilFineTuned: 0, DistilBaseEmb: 0, GPT: 0
